# CohortAI Labs V4 Phase 2

Included in this drop-in package:
- Larger, more visible brand logo treatment with stronger wordmark rendering
- Lighter premium pearl/glass theme while preserving premium contrast in hero and key cards
- More interactivity across sections and pages:
  - interactive proof strip
  - richer path reveal behavior
  - clickable track comparison canvas
  - selectable pricing ladder preview
  - interactive CTA goal chips
  - interactive About page company canvas
  - quick compare controls on Courses page
  - Contact page goal presets
- Netlify-safe structure preserved
- Build verified locally with Vite production build

Notes:
- Base directory should remain `cohortai-labs` in Netlify for your monorepo setup.
- Keep Package directory blank.
- Keep publish directory as `dist` (Netlify may display the resolved path automatically).
