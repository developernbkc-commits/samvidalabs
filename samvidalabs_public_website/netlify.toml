[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_REGISTRY = "https://registry.npmjs.org/"
  HUSKY = "0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
